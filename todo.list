//Flowchart

class engine {
private:
    std::vector<objects> render_list;


public:

    eventHandler(&args);
    update(&render_list);      //As in, update translation, velocity, etc.
    render(&render_list);      //As in, draw the objects
};






TODO:
    working-Create Object class
    working-Create Renderer
    -Make a hitbox class
        Hitbox/Object relationship:
        if (hasHitbox) {
            object->hitbox = hitbox(object::translation, object::size);
        } else {
            object->hitbox = nullptr;
        }
    -Add window handling
        -Add input handling


